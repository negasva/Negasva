// ponytail: misma página que success (ya muestra pending/fail según el estado
// real del webhook). URL distinta solo para que Meta no cuente pending como compra.
export { default } from '../success/page';
